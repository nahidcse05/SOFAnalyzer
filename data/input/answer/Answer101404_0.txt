<p>ck. in either dconf-editor > org > gnome > settings-daemon > peripherals > touchpad &amp; see if <code>scroll-method</code> is set to <code>edge-scrolling</code>. If not do so &amp; see.</p>

<p>Or System Settings > Mouse &amp; Touchpad, make sure <code>two finger scroll</code> is disabled</p>
